
local defaults = require('toggle.defaults')

describe('defaults', function()
    it('no duplicates in mappings', function()
        -- TODO
    end)

    it('no duplicates in between mappings', function()
        -- TODO
    end)
end)
